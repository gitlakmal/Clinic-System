# --- FRONTEND (Node/React) ---
node_modules/
dist/
build/
.env
.env.local
.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
# (Note: Keep package-lock.json if you want to lock versions, usually we keep it, but if it causes issues you can ignore it. For now, let's keep it tracked mostly, but exclude node_modules definitely)

# --- BACKEND (Java/Spring Boot) ---
target/
*.class
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# Logs
logs/
*.log
npm-debug.log
yarn-debug.log

# IDE & Tools (VS Code, IntelliJ, Eclipse)
.idea/
.vscode/
*.iws
*.iml
*.ipr
.classpath
.project
.settings/
.springBeans
.sts4-cache/
bin/

# OS System Files
.DS_Store
Thumbs.db